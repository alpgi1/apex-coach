spring:
  # ── Local PostgreSQL (Docker Compose) ──────────────────
  datasource:
    url: jdbc:postgresql://localhost:5432/apexcoach
    username: apexcoach
    password: apexcoach_dev
    driver-class-name: org.postgresql.Driver

  # ── Show SQL params in dev ─────────────────────────────
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.orm.jdbc.bind: TRACE
